<%= form_with(model: alias) do |form| %>
  <% if alias.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(alias.errors.count, "error") %> prohibited this alias from being saved:</h2>

      <ul>
        <% alias.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :entity_id, style: "display: block" %>
    <%= form.text_field :entity_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
